version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=CHANGE_ME_IN_PRODUCTION
      - ALLOWED_ORIGINS=https://mydomain.com
    restart: unless-stopped
